import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:flutter/material.dart';

const primaryColor = Color(0xFF007BFF);  // Bright Blue
const secondaryColor = Color(0xFF28A745);  // Light Green
const accentColor = Color(0xFFFFC107);  // Yellow
const backgroundColor = Color(0xFFF8F9FA);  // Off-White
const textColor = Color(0xFF343A40);  // Dark Gray
const buttonColor = Color(0xFFFD7E14);  // Orange
const borderColor = Color(0xFFCED4DA);  // Light Gray (Default Border)
const focusedBorderColor = Color(0xFF007BFF);  // Blue (Focused Border)
const placeholderColor = Color(0xFF393F45);  // Medium Gray (Placeholder Text)
const errorColor = Color(0xFFDC3545);  // Red (Error State)
const disabledBackgroundColor = Color(0xFFE9ECEF);  // Light Gray (Disabled Background)
const lightGrey = Color(0xFF7F8383);
const whiteColor = Color(0xFFFFFFFF);// Gray (Disabled Text)


   const Color primaryGreen = Color(0xFF549854);// Forest Green
const Color secondaryGreen=Color(0xA95EC35E);
   const Color primaryYellow = Color(0xFFFFD700); // Golden Yellow

  // Secondary Colors
   const Color secondaryBrown = Color(0xFF8B4513); // Soft Brown
   const Color secondaryBlue = Color(0xFF87CEEB); // Sky Blue

  // Accent Colors
   const Color accentLime = Color(0xFF9FD19F); // Lime Green
   const Color accentOrange = Color(0xFFFFA07A); // Light Orange

  // Neutral Colors
   const Color neutralBeige = Color(0xFFF5F5DC); // Light Beige
   const Color neutralWhite = Color(0xFFFFFFFF); // White
   const Color neutralGray = Color(0xFF36454F);
   const Color snackbarError = Color(0xFFEDA3A3);
   const Color blackColor = Color(0xFF000000);

